{
  "name": "aws-lambda-lcr",
  "version": "1.0.0",
  "description": "",
  "main": "handler.ts",
  "scripts": {
    "start": "sls offline",
    "deploy": "sls deploy --stage development --verbose",
    "deploy-force": "sls deploy --force --stage development --verbose",
    "test": "jest --coverage"
  },
  "author": "Joel Luciano",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "aws-lambda": "^1.0.7",
    "jest": "^29.5.0",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-esbuild": "^1.42.0",
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.296.0",
    "aws-sdk": "^2.1340.0",
    "redis": "^4.6.5",
    "serverless-plugin-offline-dynamodb-stream": "^1.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
